# lamoom-graphrag

Lamoom GraphRag is created based on attempts to use LlamaIndex and Microsoft's Graph Rag. After attempts to get and review a knowledge Graph realized that with using langchain i still don't have a good observability of what in the end goes inside the LLM.

We found that a lot of use-cases we faced is to generate an overview for code and for the company of tehir data; Our focus right is to generate a good Knowledge Base connected with Lamoom.com and generating a GraphRag; 

